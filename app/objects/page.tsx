import { ObjectsExplorer } from "@/components/ObjectsExplorer";

export const metadata = {
  title: "Browse — Stolen Objects",
};

export default function ObjectsPage() {
  return (
    <main>
      <ObjectsExplorer />
    </main>
  );
}
